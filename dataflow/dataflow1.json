{
	"name": "dataflow1",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "CountriesForTesting",
						"type": "DatasetReference"
					},
					"name": "CountriesForTesting"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "PostgreSQL_Countries",
						"type": "DatasetReference"
					},
					"name": "sink1"
				}
			],
			"transformations": [
				{
					"name": "flatten1"
				}
			],
			"scriptLines": [
				"source(output(",
				"          countries as (ABW as (label as string), AFG as (label as string), AGO as (label as string), AIA as (label as string), ALB as (label as string), ARE as (label as string), ARG as (label as string), ARM as (label as string), ASM as (label as string), ATG as (label as string), AUS as (label as string), AUT as (label as string), AZE as (label as string), BDI as (label as string), BEL as (label as string), BEN as (label as string), BFA as (label as string), BGD as (label as string), BGR as (label as string), BHR as (label as string), BHS as (label as string), BIH as (label as string), BLR as (label as string), BLZ as (label as string), BMU as (label as string), BOL as (label as string), BRA as (label as string), BRB as (label as string), BRN as (label as string), BTN as (label as string), BWA as (label as string), CAF as (label as string), CAN as (label as string), CHE as (label as string), CHI as (label as string), CHL as (label as string), CHN as (label as string), CIV as (label as string), CMR as (label as string), COD as (label as string), COG as (label as string), COK as (label as string), COL as (label as string), COM as (label as string), CPV as (label as string), CRI as (label as string), CUB as (label as string), CYM as (label as string), CYP as (label as string), CZE as (label as string), DEU as (label as string), DJI as (label as string), DMA as (label as string), DNK as (label as string), DOM as (label as string), DZA as (label as string), ECU as (label as string), EGY as (label as string), ERI as (label as string), ESH as (label as string), ESP as (label as string), EST as (label as string), ETH as (label as string), FIN as (label as string), FJI as (label as string), FLK as (label as string), FRA as (label as string), FRO as (label as string), FSM as (label as string), GAB as (label as string), GBR as (label as string), GEO as (label as string), GHA as (label as string), GIB as (label as string), GIN as (label as string), GLP as (label as string), GMB as (label as string), GNB as (label as string), GNQ as (label as string), GRC as (label as string), GRD as (label as string), GRL as (label as string), GTM as (label as string), GUF as (label as string), GUM as (label as string), GUY as (label as string), HKG as (label as string), HND as (label as string), HRV as (label as string), HTI as (label as string), HUN as (label as string), IDN as (label as string), IMY as (label as string), IND as (label as string), IRL as (label as string), IRN as (label as string), IRQ as (label as string), ISL as (label as string), ISR as (label as string), ITA as (label as string), JAM as (label as string), JOR as (label as string), JPN as (label as string), KAZ as (label as string), KEN as (label as string), KGZ as (label as string), KHM as (label as string), KIR as (label as string), KNA as (label as string), KOR as (label as string), KWT as (label as string), LAO as (label as string), LBN as (label as string), LBR as (label as string), LBY as (label as string), LCA as (label as string), LIE as (label as string), LKA as (label as string), LSO as (label as string), LTU as (label as string), LUX as (label as string), LVA as (label as string), MAC as (label as string), MAR as (label as string), MCO as (label as string), MDA as (label as string), MDG as (label as string), MDV as (label as string), MEX as (label as string), MHL as (label as string), MKD as (label as string), MLI as (label as string), MLT as (label as string), MMR as (label as string), MNE as (label as string), MNG as (label as string), MNP as (label as string), MOZ as (label as string), MRT as (label as string), MSR as (label as string), MTQ as (label as string), MUS as (label as string), MWI as (label as string), MYS as (label as string), MYT as (label as string), NAM as (label as string), NCL as (label as string), NER as (label as string), NGA as (label as string), NIC as (label as string), NIU as (label as string), NLD as (label as string), NOR as (label as string), NPL as (label as string), NRU as (label as string), NZL as (label as string), OMN as (label as string), PAK as (label as string), PAN as (label as string), PCN as (label as string), PER as (label as string), PHL as (label as string), PLW as (label as string), PNG as (label as string), POL as (label as string), PRI as (label as string), PRK as (label as string), PRT as (label as string), PRY as (label as string), PYF as (label as string), QAT as (label as string), REU as (label as string), ROU as (label as string), RUS as (label as string), RWA as (label as string), SAU as (label as string), SDN as (label as string), SEN as (label as string), SGP as (label as string), SHN as (label as string), SJM as (label as string), SLB as (label as string), SLE as (label as string), SLV as (label as string), SMR as (label as string), SOM as (label as string), SPM as (label as string), SRB as (label as string), STP as (label as string), SUR as (label as string), SVK as (label as string), SVN as (label as string), SWE as (label as string), SWZ as (label as string), SYC as (label as string), SYR as (label as string), TCA as (label as string), TCD as (label as string), TGO as (label as string), THA as (label as string), TJK as (label as string), TKL as (label as string), TKM as (label as string), TLS as (label as string), TON as (label as string), TTO as (label as string), TUN as (label as string), TUR as (label as string), TUV as (label as string), TWN as (label as string), TZA as (label as string), UGA as (label as string), UKR as (label as string), URY as (label as string), USA as (label as string), UZB as (label as string), UVK as (label as string), VAT as (label as string), VCT as (label as string), VEN as (label as string), VGB as (label as string), VIR as (label as string), VNM as (label as string), VUT as (label as string), WBG as (label as string), WLF as (label as string), WSM as (label as string), YEM as (label as string), ZAF as (label as string), ZMB as (label as string), ZWE as (label as string), SSD as (label as string), BNR as (label as string), CUW as (label as string), MFX as (label as string), SXM as (label as string), ATI as (label as string), ATL as (label as string), AND as (label as string)),",
				"          api as (version as string, {output-method} as string)",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     ignoreNoFilesFound: false,",
				"     documentForm: 'documentPerLine') ~> CountriesForTesting",
				"CountriesForTesting foldDown(unroll(),",
				"     mapColumn(",
				"          countries,",
				"          api",
				"     ),",
				"     skipDuplicateMapInputs: false,",
				"     skipDuplicateMapOutputs: false) ~> flatten1",
				"flatten1 sink(allowSchemaDrift: true,",
				"     validateSchema: true,",
				"     deletable:false,",
				"     insertable:true,",
				"     updateable:false,",
				"     upsertable:false,",
				"     format: 'table',",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> sink1"
			]
		}
	}
}